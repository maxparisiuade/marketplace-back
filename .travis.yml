language: python
python:
  - "3.10"

# Instalar Poetry
before_install:
  - pip install poetry

# Instalar dependencias usando Poetry
install:
  - poetry install

# Ejecutar pruebas
script:
  - poetry run pytest test